[project]
name = "dbot"
version = "1.0.0"
description = "AI-powered medical assistant for recording, analyzing, and retrieving patient data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "flask-cors>=4.0",
    "google-genai>=2.0",
    "groq>=1.0",
    "gunicorn>=22.0",
    "pandas>=2.0",
    "pyaudio>=0.2.14",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false